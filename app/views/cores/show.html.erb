<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @core.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @core.description %>
</p>

<p>
  <strong>Creator:</strong>
  <%= @core.creator %>
</p>
<p>
  <strong>Core Members:</strong>
<%= render partial: 'core_memberships/core_memberships', locals: { core_memberships: @core.core_memberships } %>
<%= link_to 'Add New Member', new_core_core_membership_path(@core) if can? :create, @core.core_memberships.build %>
</p>
<%= link_to 'Back', cores_path %>
