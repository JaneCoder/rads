<h1>Listing pipeline users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <%= render partial: 'user_administration_panel', locals: {user: user} %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= create_link User %>
