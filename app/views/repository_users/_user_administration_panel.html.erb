<td><%= show_link user %></td>
<td><%= edit_link user %></td>
<td>
  <% if can? :destroy, user %>
  <%= user.is_enabled? ?
        destroy_link(user, 'Disable') :
        link_to('Enable', repository_user_path(user, repository_user: {is_enabled: true}), :method => :patch, :data => { :confirm => "Are you sure?" }) %>
  <% end %>
</td>

